#include <stdio.h>
#include <conio.h>
int main()
{
 float a, b, c, d, e, per;
 clrscr();
 printf("enter the marks of 5 subjects/h");
 scanf("%F%F%F%F%F",&a,&b,&c,&d,&e);
 per=(a+b+c+d+e)/5

    printf("percentage is % 2f /n,per");
    if (per >90 &&per <=100)
    {
        printf("grade is A");
    }
    else if ( per >80 && per <=90)
    {printf("grade is b"):
    }
  getch();
  return 0;
    }

    return 0;
}